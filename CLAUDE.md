# Project Overview: rhosocial-activerecord-sqlserver

## Project Name
- **Repository Name**: python-activerecord-sqlserver
- **Python Package Name**: rhosocial-activerecord-sqlserver

## WARNING

**This project is currently under active development and is NOT ready for production use.**

- **DO NOT use in testing environments**
- **DO NOT use in production environments**
- **DO NOT rely on any functionality as it may change without notice**

## Project Purpose

This project is a SQL Server backend implementation for the `rhosocial-activerecord` Python package. It provides Microsoft SQL Server database support with the elegant ActiveRecord pattern interface.

## Key Design Principles

1. **Backend Implementation**: Extends core ActiveRecord with SQL Server-specific features
2. **Driver**: Uses `pyodbc` for database connectivity
3. **Namespace Package**: Integrates with the rhosocial namespace package architecture

## Current Status

This project is in early development. Key features being implemented:

- Basic CRUD operations
- Connection management
- Transaction support
- Schema introspection
- SQL Server-specific data types

## Version Control and Changelog

This project adheres to the same version control, branching, commit message, and changelog management standards as the main `python-activerecord` project.
