Added `SQLServerDialect.create_schema_differ()` returning `SQLServerSchemaDiffer` so the dialect exposes its ordinal-position-aware schema comparison through the core's new dependency-inversion hook (`SQLDialectBase.create_schema_differ()`), keeping migration snapshot diffs correct now that the core no longer imports concrete backend differs.
