Removed lint-only development tools (mypy, black, isort, ruff) from `requirements-dev.txt`; these remain available via the `dev` extra in `pyproject.toml`. This allows the CI matrix, including free-threaded Python job (3.13t + SQL Server 2019), to install dependencies successfully.