Added `--scenarios` option support: scenario filtering now reads the generic `TESTSUITE_ACTIVE_SCENARIOS` env var (or the backend-local `SQLSERVER_ACTIVE_SCENARIOS`) so tests run consistently from both the testsuite directory and the backend's own `tests/` directory. Raised the core dependency floor to `rhosocial-activerecord>=1.0.0.dev0`.