# requirements.txt
# Backend-specific dependencies only
# Core dependency (rhosocial-activerecord) is installed via CI workflow from git

# SQL Server driver (sync)
pyodbc>=5.3.0

# SQL Server async driver (required by AsyncSQLServerBackend)
aioodbc>=0.5.0
