# schema tests

SQL Server schema support: the SchemaSupport capability (native named schema namespaces such as dbo) with DDL formatting, and the SQL Server schema differ.

## Key files

- `test_schema_diff.py` — SQLServerSchemaDiffer
- `test_schema_support.py` — SchemaSupport capability and DDL formatting
